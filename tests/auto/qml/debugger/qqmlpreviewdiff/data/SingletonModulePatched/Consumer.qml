import QtQuick

Rectangle {
    width: 200
    height: 200
    color: Colors.background

    property color currentBackground: Colors.background
    property color currentTextColor: Colors.textColor
}
