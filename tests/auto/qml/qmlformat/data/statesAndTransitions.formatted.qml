QtObject {
    id: foo

    // This needs to be *before* states and transitions after formatting
    Item {
    }

    states: [
        State {
        }
    ]
    transitions: [
        Transition {
        }
    ]
}
